name: wavyos
description: Debug Build
base-image: quay.io/fedora-asahi-remix-atomic-desktops/silverblue
image-version: 42

modules:
  - type: files
    files:
      - source: system
        destination: /

  - type: rpm-ostree
    install:
      - fish
      - zsh
      - ptyxis

  # Temporarily removed: Fonts, Brew, Flatpaks, Systemd
